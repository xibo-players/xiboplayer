name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '17 3 * * *'  # nightly at 03:17 UTC
  workflow_dispatch:

jobs:
  unit:
    uses: xiboplayer/.github/.github/workflows/test.yml@4ce2b2d7d1af170708f94ffa4a3c9c379a161f95  # main
    with:
      lint-command: 'pnpm lint'
      typecheck: true

  integration:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: unit
    uses: xiboplayer/.github/.github/workflows/integration-test.yml@4ce2b2d7d1af170708f94ffa4a3c9c379a161f95  # main
    with:
      # Runs against the self-contained CMS in docker-compose.test.yml.
      # Pointing this at xibo-dev.superpantalles.com made the job fail with
      # ENOTFOUND: that host has no public DNS record or ingress.
      cms-url: ''
    secrets:
      CMS_CLIENT_ID: ${{ secrets.CMS_CLIENT_ID }}
      CMS_CLIENT_SECRET: ${{ secrets.CMS_CLIENT_SECRET }}
